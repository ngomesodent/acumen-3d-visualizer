type ProjectPaths = {
  scenarios: string;
};

type ProjectFilenames = {
  scenario: string;
};

export const paths: ProjectPaths = {
  scenarios: "/scenarios/",
};

export const filenames: ProjectFilenames = {
  scenario: "NYCTaxiTrip",
};